extends layout

block head
    link(rel="stylesheet" href="/css/dashboard.css")

block content
    main.dashboard-container
        .dashboard-header
            h1 Admin Dashboard
            p Welcome, #{user.full_name}!

        .dashboard-content
            .dashboard-grid
                .dashboard-card
                    .card-icon USERS
                    h3 Total Users
                    p #{stats.users} Users
                    a.btn-secondary(href="/admin/users") Manage Users

                .dashboard-card
                    .card-icon TUTORS
                    h3 Total Tutors
                    p #{stats.tutors} Tutors
                    a.btn-secondary(href="/admin/users?role=tutor") Manage Tutors

                .dashboard-card
                    .card-icon STUDENTS
                    h3 Total Students
                    p #{stats.students} Students
                    a.btn-secondary(href="/admin/users?role=tutee") Manage Students

                .dashboard-card
                    .card-icon(style="color: #ff4757") FLAGGED
                    h3 Flagged Tutors
                    p #{stats.flagged || 0} Flagged
                    a.btn-secondary(href="/admin/flagged" style="border-color: #ff4757; color: #ff4757") Review Flags

            .dashboard-section
                h2 System Management
                .admin-menu
                    a.admin-btn(href="/admin/users") Manage All Users
                    a.admin-btn(href="/admin/approve-tutors") Approve Tutors
                    a.admin-btn(href="/admin/flagged" style="background: #fff5f5; color: #ff4757; border-color: #ff4757") Review Flagged Tutors


