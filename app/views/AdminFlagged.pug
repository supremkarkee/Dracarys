extends Layout

block head
    link(rel="stylesheet" href="/css/dashboard.css")

block content
    main.dashboard-container
        .dashboard-header
            h1 Flagged Tutors
            p Review and manage tutors who have been flagged by students.

        .dashboard-content
            .dashboard-section
                h2 Flagged Profiles
                .users-table
                    if flaggedTutors && flaggedTutors.length
                        table
                            thead
                                tr
                                    th Tutor Name
                                    th Email
                                    th Flag Count
                                    th Actions
                            tbody
                                each tutor in flaggedTutors
                                    tr
                                        td= tutor.full_name
                                        td= tutor.email
                                        td
                                            span.flag-count-badge #{tutor.flag_count} Flags
                                        td
                                            a.btn-action-view(href=`/tutor/${tutor.tutor_id}`) View Profile
                                            button.btn-resolve.btn-action-resolve(data-tutor-id=tutor.tutor_id) Dismiss Flags
                                            button.btn-danger.btn-action-danger(data-user-id=tutor.user_id) Delete Account
                    else
                        p No tutors are currently flagged.

block scripts
    script.
        // Dismiss Flags
        document.querySelectorAll('.btn-resolve').forEach(btn => {
            btn.addEventListener('click', async function() {
                const tutorId = this.dataset.tutorId;
                if (confirm('Are you sure you want to dismiss all flags for this tutor?')) {
                    try {
                        const response = await fetch(`/admin/flagged/resolve/${tutorId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' }
                        });
                        const data = await response.json();
                        if (data.success) {
                            location.reload();
                        } else {
                            alert('Error: ' + data.error);
                        }
                    } catch (err) {
                        alert('Error: ' + err.message);
                    }
                }
            });
        });

        // Delete Account
        document.querySelectorAll('.btn-danger').forEach(btn => {
            btn.addEventListener('click', async function() {
                const userId = this.dataset.userId;
                if (confirm('CAUTION: This will permanently delete the tutor account and all associated data. Continue?')) {
                    try {
                        const response = await fetch(`/admin/users/delete/${userId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' }
                        });
                        const data = await response.json();
                        if (data.success) {
                            alert('Tutor account deleted successfully');
                            location.reload();
                        } else {
                            alert('Error: ' + data.error);
                        }
                    } catch (err) {
                        alert('Error: ' + err.message);
                    }
                }
            });
        });
